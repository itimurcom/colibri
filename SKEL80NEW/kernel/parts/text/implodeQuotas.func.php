<?
//..............................................................................
// возвращает строчку через зяпятую в кавычках
//..............................................................................
function implodeQuotas($arr)
	{
	if (!is_array($arr)) return;
	$csv = implode(',', $arr);
        return "'" . str_replace(",", "','", $csv) . "'";
	}
?>